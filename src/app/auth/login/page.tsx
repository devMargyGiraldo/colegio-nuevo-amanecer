import { Login } from '@/components/auth/login';

const LoginPage = () => {
  return (
    <div className="flex min-h-screen items-center p-2">
      <Login />
    </div>
  );
};

export default LoginPage;
